using System;

namespace UnitTestPracticeHomework
{
    class Program
    {
        static void Main(string[] args)
        {
            /* double[] numbers;
            numbers = new double[] { 10, 5, 25, 30, 9 };

            string[] operators;
            operators = new string[] { "*", "+", "-", "/" };

            var doMath = new Operations();
            var result = doMath.DoOperation(numbers, operators);
            Console.WriteLine("The result of the operations is: " + result); */
        }
    }
}
